@using ERP.Core.Models
@model Vendor

@{
    ViewData["Title"] = "Vendor Details";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="fw-bold mb-1">Vendor Details</h4>
        <p class="text-muted mb-0">@Model.VendorName</p>
    </div>
    <a asp-controller="Vendors" asp-action="Index" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Back to List
    </a>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body p-4">
        <div class="row g-4">
            <div class="col-md-6">
                <p class="text-muted mb-1">Vendor Name</p>
                <h6 class="fw-bold">@Model.VendorName</h6>
            </div>
            <div class="col-md-6">
                <p class="text-muted mb-1">Contact Person</p>
                <h6 class="fw-bold">@Model.ContactPerson</h6>
            </div>
            <div class="col-md-6">
                <p class="text-muted mb-1">Email</p>
                <h6 class="fw-bold">@Model.Email</h6>
            </div>
            <div class="col-md-6">
                <p class="text-muted mb-1">Phone</p>
                <h6 class="fw-bold">@Model.Phone</h6>
            </div>
            <div class="col-md-6">
                <p class="text-muted mb-1">GST Number</p>
                <h6 class="fw-bold">@Model.GSTNumber</h6>
            </div>
            <div class="col-md-6">
                <p class="text-muted mb-1">Address</p>
                <h6 class="fw-bold">@Model.Address</h6>
            </div>
        </div>
        <div class="mt-4">
            <a href="/Vendors/Edit/@Model.Id" class="btn btn-primary me-2">
                <i class="fas fa-edit me-2"></i>Edit Vendor
            </a>
            <a asp-controller="Vendors" asp-action="Index"
               class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>Back to List
            </a>
        </div>
    </div>
</div>